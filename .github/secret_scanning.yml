# Secret scanning configuration.
#
# GitHub auto-enables secret scanning for public repos. This config file
# is the public-visible marker that secret scanning is configured for
# this repo (the API-level state is admin-only, so a public scanner
# can't see it without auth).
#
# No paths are excluded — secret scanning runs on the full tree.
paths-ignore: []
